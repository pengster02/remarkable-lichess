# Project Guidance

## Comments

Keep comments minimal. Do not explain what code does — only add a comment when the WHY is genuinely non-obvious (a hidden platform constraint, a workaround for a specific confirmed bug, a real invariant a future editor could easily break). Default to zero comments per change. Prefer a short one-liner over a multi-line block.
